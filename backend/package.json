{
  "name": "aspira-backend",
  "version": "1.0.0",
  "description": "Backend for Aspira web application",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'No build step needed for backend'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-static-gzip": "^2.1.7",
    "mongoose": "^8.9.7",
    "nodemailer": "^6.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
